@{
    ViewBag.Title = "Track Claims";
}

<div class="d-flex justify-content-center align-items-center vh-100">
    <div class="text-center">
        <h1>Track Your Claims</h1>
        <h3>Select an option below:</h3>

        <div class="btn-group" role="group" aria-label="Track claims actions">
            <a href="@Url.Action("ViewRejectedClaims", "Coordinator")" class="btn btn-danger">View Rejected Claims</a>
            <a href="@Url.Action("ViewVerifiedClaims", "Coordinator")" class="btn btn-success">View Verified Claims</a>
        </div>

        <div class="mt-4">
            <a href="@Url.Action("Index", "Coordinator")" class="btn btn-primary back-btn">Back to Dashboard</a>
        </div>
    </div>
</div>

<style>
    .btn {
        margin: 10px;
        width: 200px;
    }

    .back-btn {
        width: 410px;
    }
</style>
